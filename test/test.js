const assert = require('chai').assert;

describe('Core functionality', function() {

  it('User should see stations they are close to', function() {

  })

  it('User should be able to click on a station and see \
  arriving train times', function() {
    
  })
})